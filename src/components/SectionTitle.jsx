const SectionTitle = ({ text }) => {
  return (
    <div className="border-b border-base-300 pb-5">
      <h2 className="font-medium text-3xl tracking-wider capitalize">{text}</h2>
    </div>
  );
};

export default SectionTitle;
